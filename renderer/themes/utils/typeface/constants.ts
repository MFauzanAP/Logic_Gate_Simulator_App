//	Declare formatting regex
const formatRegex = /^(\$[a-z]+[0-9]{1}0{2})$/;
const nameRegex = /([a-z]+)/;
const sizeRegex = /([0-9]{1}0{2})/;

//  Exports
export {
	formatRegex,
	nameRegex,
	sizeRegex,
};
