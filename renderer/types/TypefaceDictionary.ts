//	Imports
import type * as Stitches from '@stitches/react';

//	Declare typeface dictionary type
type TypefaceDictionary = {
	readonly [key: string]		: Stitches.CSS,
}

//	Exports
export default TypefaceDictionary;
